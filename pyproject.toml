[project]
name = "meetmind"
version = "0.1.0"
description = "FastAPI + Postgres starter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "alembic>=1.14.0",
    "pydantic-settings>=2.7.0",
    "bcrypt>=4.0.0",
    "psycopg2-binary>=2.9.12",
    "python-jose>=3.5.0",
    "resend>=2.30.0",
    "Jinja2>=3.1.0",
    "httpx>=0.28.1",
    "slowapi>=0.1.9",
    "mako>=1.3.12",
    "redis>=7.4.0",
    "urllib3>=2.7.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.28.0",
    "aiosqlite>=0.22.1",
    "ruff>=0.15.12",
    "pre-commit>=4.6.0",
    "pip-audit>=2.10.0",
]

[tool.ruff]
exclude = [
    ".venv",
    "alembic/versions/**",
    "alembic/env.py",
]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --tb=short"
